/**
 * Public API for the History feature.
 *
 * Currently consumed only by App.tsx (via React.lazy import to the page
 * file directly — lazy needs a module with a default export).
 *
 * This feature has no exported hooks or helpers yet. It exists to
 * establish the feature boundary and make it obvious where history/
 * timeline code lives.
 */

export {}
